# KubernetesClusterView

**GeneralView** exposing `KubernetesCluster`.

## What you see

All three planes (compute, storage, network) inside the cluster part, cluster-level ports, and internal connections linking aggregate ports to plane ports. Use this view to understand how orchestrator and monitoring reach individual planes.
