# Router

**Edge router** connecting site LAN to WAN (`lan`, `wan`, `mgmt`, `power`, `cool`).

## Allocation

Northbound logical API (`orchestrator_rap`), IaaC provisioning, and external monitoring paths terminate on LAN/management ports.
