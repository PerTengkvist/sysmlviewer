# ServerBladeView

**GeneralView** exposing `ServerBlade`.

## What you see

Internal hardware decomposition and fabric wiring inside a blade—useful when tracing `compute_svp` allocation to CPU fabric.
